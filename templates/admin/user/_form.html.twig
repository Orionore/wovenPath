{{ form_start(form) }}
<div class="row mb-3">
    <div class="col">
        {{ form_row(form.roles) }}
    </div>
</div>
<button class="btn btn-author">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}