{{ form_start(form) }}
<div class="row mb-3">
    <div class="col">
        {{ form_row(form.roles) }}
    </div>
</div>
<div class="row mb-3">
    <div class="col">
        {{ form_row(form.isVerified) }}
    </div>
</div>
<button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}